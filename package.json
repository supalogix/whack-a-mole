{
  "name": "bottomline-poc",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "serve": "http-server ./dist",
    "test": "mocha --compilers=babel-core packages/tests/*.spec.js"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0",
    "babel-cli": "^6.26.0",
    "babelify": "^10.0.0",
    "browserify": "^16.2.3",
    "chai": "^4.2.0",
    "http-server": "^0.11.1",
    "mocha": "^5.2.0"
  }
}
